---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<article
  class={`bg-black/70 backdrop-blur-sm w-full text-white shadow-lg flex flex-col items-center p-4 rounded-xl text-2xl ${className}`}
>
  <slot />
</article>
