---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<article
  class={`w-full text-white flex flex-col items-center rounded-xl text-2xl ${className}`}
>
  <slot />
</article>
